import React from 'react';
import Navbar from '../Navbar';

const Schedule = () => {
  return (
    <div>
    <Navbar/>
    <h1>หน้าจัดตารางของ ผู้จัดตาราง</h1>
  </div>
  );
};

export default Schedule;